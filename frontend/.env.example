# URL de base de l'API backend (FastAPI)
VITE_API_BASE_URL=http://localhost:8000/api/v1

# Lien de paiement Stripe (Payment Link) pour l'upgrade Premium/BtoB.
# IMPORTANT : dans le dashboard Stripe, configure la redirection "après
# paiement" de ce Payment Link vers `${VITE_APP_URL}/success` (ex:
# https://app.subserver.com/success) pour que le retour utilisateur
# déclenche la mise à jour isPremium côté frontend (cf. SuccessPage.tsx).
VITE_STRIPE_BILLING_URL=https://buy.stripe.com/cNi28t3y0gnz5gvcDOds400
